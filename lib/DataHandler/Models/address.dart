class Address {
  var placeFormattedAddress;
  var placeName;
  var placeId;
  var latitude;
  var longitude;
  Address(
      {this.placeFormattedAddress,
      this.placeName,
      this.placeId,
      this.latitude,
      this.longitude});
}
