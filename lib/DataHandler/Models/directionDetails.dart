class DirectionDetails {
  var distanceValue;
  var durationValue;
  var distanceText;
  var durationText;
  var encodedPoints;

  DirectionDetails(
      {this.distanceValue,
      this.durationValue,
      this.distanceText,
      this.durationText,
      this.encodedPoints});
}
